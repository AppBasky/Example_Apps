template: 'python'
framework: 'flask'
image: 'python:3.7-buster'
env:
  - name: APP_NAME
    value: "Flask-Example"
command:
  - pip install -r requirements.txt
  - gunicorn --bind=0.0.0.0 --workers=4 startup:app